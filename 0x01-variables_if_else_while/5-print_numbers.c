#include <stdio.h>

/**
 * main - prints number from 0 to 10
 *
 * Return: always 0 (default)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
